int f(int a, int x) {
    return 1;
}

int main() {
    int x;
    set s;

    float a;

    if(a) {}

    if (remove(1 in add(1 in s)) == EMPTY) {
        return 0;
    }

    for(x=0; x< 1.0; x = x+1.2 ){
        return 1.7;
    }

    f(1, 2);

    a+1.2*4+x/2;

    return 0;
}