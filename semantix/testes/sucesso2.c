float x(){
  return 1;
}

int y(float a, float b){
  return a+b;
}

int main() {
  int a;
  float b;
  set s;
  elem el;
  add(1 in s);

  remove(1 in s);
  remove(exists (el in s) in s);

  if(a == a) {
    for(a = 1; a < 1.2; a = a + b) {
      a = b;
      y(a, a);
    }
  }

  if(x()) {}
  el = 1;
  return el;
}