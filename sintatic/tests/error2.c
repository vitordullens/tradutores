// Global var
int k;

int f(int x) {
    int float a, b;   // Teste
    float pi;
    set s;
    elem el;
    a = 1;
    b = 2;
    return a + b;
    {
        // Inner scope
        int d;
    }
}

int aaaaa(int a, int b; int c){
    ;remove(exists(el in s) in s);
    add(1 in add( 2 in add(2 in s)));
    err
}

// Main
int main() {
    int a;
    int b;;
}