// Global var
int k;

int f(int y) {
    set s;
    return s + el;
    elem el;
    int x;
}


int outrafuncao(int a, int b, int c){
    for(i=0;i<10;i = i +1 ) {
        return 1;
    }
}
int main() {
    float a;
    if (a<b) {
        add((exists (el in s)) in s);
    }
    int b;
}