// Global var
int k;

int f(int x) {
    int a; 
    int b;   // Teste
    float pi;
    set s;
    elem el;
    a = 1;
    b = 2;
    return a + b;
}

int aaaaa(int a, int b, int c){
    alolao(teste(b), a);
}

// Main
int main() {
    int a;
    int b;
    if (1) {
        int a;
        remove((exists (el in s)) in s);
    }
}